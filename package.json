{
  "name": "together",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@material-ui/core": "^4.5.2",
    "@material-ui/icons": "^4.5.1",
    "@material-ui/lab": "^4.0.0-alpha.30",
    "axios": "^1.6.0",
    "enumify": "^1.0.4",
    "password-meter": "^3.1.0",
    "react": "^16.11.0",
    "react-dom": "^16.11.0",
    "react-router": "^5.1.2",
    "react-router-dom": "^5.1.2",
    "react-scripts": "3.1.1"
  },
  "devDependencies": {
    "archiver": "^3.1.1",
    "chalk": "^3.0.0",
    "fs-extra": "^8.1.0",
    "gulp": "^4.0.2",
    "gulp-ssh": "^0.7.0"
  },
  "scripts": {
    "start": "gulp useDevConfig --silent && react-scripts start",
    "build": "gulp useProdConfig --silent && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "gulp useProdConfig --silent && react-scripts build && gulp deploy --silent"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
